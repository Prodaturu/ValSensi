# Valorant-Sensitivity-Identifier

Frontend-only website to help find the correct in-game sensitivity based on overshooting and undershooting the displayed targets.

## Initial web app setup

This project now uses a minimal Vite setup.

### Run locally

```bash
npm install
npm run dev
```

### Build

```bash
npm run build
```
